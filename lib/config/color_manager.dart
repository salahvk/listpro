import 'package:flutter/material.dart';

class ColorManager {
  static const primary = Color.fromARGB(118, 4, 0, 122);
  static const textColor = Colors.white;
  static const lightBlue = Color.fromARGB(255, 141, 204, 233);
}
